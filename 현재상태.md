# 현재 상태 · 이어서 할 일

**대상: 경기도 의정부시 전역** · 갱신 2026-08-13

대화가 끊기거나 다음에 다시 시작할 때 **이 문서부터 읽으면 된다.**

---

## 한 줄 요약

**데이터 파이프라인이 끝났다.** 그래프·맨홀·침수가 전부 DB에 있고
특보 4단계가 모두 실제로 경로를 바꾼다. 이제 **C(API)와 D(앱)** 차례다.

---

## 지금 어디까지 됐나

| 단계 | 상태 | 결과 | 담당 |
|---|---|---|---|
| 0 환경 구축 | 완료 | PostgreSQL 17.5 / PostGIS 3.5 / pgRouting 3.7.3 | A |
| 1 스키마 | 완료 | `manhole` · `flood_zone` · `risk_segment` | A |
| 2 보행 그래프 | 완료 | **엣지 39,490 / 노드 29,744 / 2,760km** | A |
| 3 침수 적재 | **완료** | 3종 34개 폴리곤 · EPSG:4326 | **B** |
| 4 맨홀 적재 | 완료 | **20,873개 · 스냅률 94.9%** | A |
| 5 위험도 계산 | **완료** | **9,690구간** (침수 4,313 / 맨홀 7,357) | **B** |
| 6 경로 함수 | 완료 | 37ms | A |
| 7 API | **여기부터** | | C |
| — 앱 | **여기부터** | | D |
| — 진동 키링 | 설계 완료 | `warning_manhole` 뷰 5,975개 · 펌웨어 · 부품 목록 | A+D |

### 실제로 회피가 되는 것을 확인함

의정부역 → 회룡역. **위험 판정 구간**은 `min_freq <= alert` 인 것만 센다.

```
특보         거리      위험구간 통과   맨홀
평상시      2,377m         0m          6
호우주의보  3,107m         0m ✅       14
호우경보    3,151m         0m ✅       14
극한호우    3,150m       426m          15
```

**4단계가 전부 살아 있다.** 극한호우의 426m는 모든 빈도가 켜지면 우회로가
없어서 그렇다 — 버그가 아니라 설계에 적어둔 경계 사례다. 이때는 최소 위험
경로를 주되 **위험을 지난다는 사실을 앱이 표시**한다.

맨홀이 6→14개로 늘어나는 것도 정상이다. 침수를 피하려고 맨홀이 촘촘한
골목으로 돌기 때문이고, 둘을 같은 저울에 올려 합이 작은 쪽을 고른 결과다.

### 침수 데이터 빈도 구성

```
urban            30 · 100 · 500 · 9999      2.88 ~ 3.51 km²
national_river  100 · 200 · 500            0.01 ~ 0.26 km²
local_river     100                        1.68 km²   ← 낮은 빈도는 포털에 없음
```

지방하천 50·80년은 **포털에 자료 자체가 없다.** 도시침수 30년이 호우주의보
단계를 커버하고 있어 4단계 작동에는 지장이 없다. 알려진 한계로 둔다.

---

## 파일이 어디 있나

### 로컬 (이 폴더)

```
sql/                    파이프라인 스크립트 (깃허브에도 있음)
data/floodnav.dump      6.2MB · 팀 배포용        ← 깃허브에 없음
data/manhole_utf8.csv   936KB · 맨홀 원본        ← 깃허브에 없음
data/south-korea-latest.osm.pbf  272MB · OSM 원본 ← 깃허브에 없음
```

### 깃허브 (비공개)

https://github.com/qkrtndus5865-web/manhole-flood-nav

`sql/` + 문서만. **데이터는 없다** — 깃은 파일의 모든 버전을 영원히 보관해서
6.2MB 덤프를 열 번 갱신하면 그게 다 저장소에 쌓인다.

### 팀 문서 (아티팩트)

| 문서 | 내용 |
|---|---|
| 동작 원리 | 이 앱이 어떻게 돌아가는가 · 설계 결정과 근거 |
| 제작 가이드 | 환경 구축부터 API까지 단계별 절차 |

### 아직 안 올린 것

- **`data/floodnav.dump` → 구글 드라이브** ← C·D가 이걸 기다린다

---

## 접속 정보

```
localhost:5432 · db floodnav · user postgres · pw flood1234

docker start flood-db                                    컨테이너 켜기
docker exec -i flood-db psql -U postgres -d floodnav < sql/99_check.sql    점검
```

로컬 개발용 비밀번호다. 배포할 때 바꾼다.

---

## 역할 — 경계는 테이블이다

| | 담당 | 산출물 |
|---|---|---|
| **A** | 그래프 · 맨홀 · 경로 함수 | `ways` · `manhole` · `route_summary()` |
| **B** | **침수 적재 + 위험도 계산** | **`flood_zone` · `risk_segment`** |
| **C** | API 서버 | `POST /v1/routes` |
| **D** | 앱 | Flutter + OSM 타일 |

```
A: 덤프 v1 (그래프+맨홀) ──→ B: flood_zone 적재 + 04_risk 실행 ──→ 덤프 v2 ──→ C·D
                                                          ✅ 완료 2026-08-13
```

한 방향으로만 흐른다. 데이터 단계는 여기서 끝났고, 이후 데이터가 갱신될 때만
같은 경로를 다시 탄다.

---

## 다음에 할 일

### A (나)

1. **`floodnav_v2.dump` 복원 후 `05_route.sql` 실행** ← 덤프 안 함수가 옛날 버전이다
2. 그 결과를 **구글 드라이브에 올리고 링크 공유** ← C·D가 기다린다
3. C가 API 만들 때 막히면 뚫어주기
4. 여유 생기면 D(Flutter) 백업

### B — 검수 완료 (2026-08-13)

`floodnav_v2.dump` 검사 결과. **전부 통과했다.**

| 검사 | 결과 |
|---|---|
| 좌표계 · 도형 | EPSG:4326 / MultiPolygon ✅ |
| 종류 이름 | `urban` · `local_river` · `national_river` ✅ |
| `risk_segment` | 9,690행 (침수 4,313 / 맨홀 7,357) ✅ |
| 침수인데 맨홀 0인 행 | **2,333개** — 배치를 제대로 돌린 증거 ✅ |
| 기존 자산 보존 | 엣지·노드·맨홀·스냅률 전부 그대로 ✅ |
| `99_check.sql` | 6항목 전부 통과 ✅ |

**v1에서 지적한 것 두 가지 모두 수정됨.** `regional_river` → `local_river`,
그리고 `04_risk.sql` 미실행으로 침수 전용 구간 2,149개가 누락됐던 문제.

**빈도도 추가로 받아왔다** — 도시침수 30·500·9999, 국가하천 200·500.
특히 **도시침수 30년**이 들어오면서 호우주의보 단계가 살아났다.

> ⚠️ **덤프에는 함수도 같이 들어간다.** 복원하면 B가 갖고 있던 옛날
> `route_safe`(13 파라미터)가 딸려온다. 복원 후 `05_route.sql`을 반드시
> 다시 돌려야 맨홀 강우 배수가 적용된다.

**`flood_zone` 스키마 약속** (앞으로 데이터가 늘어도 이대로)

| 칼럼 | 값 |
|---|---|
| `geom` | MultiPolygon, **EPSG:4326** |
| `flood_type` | `urban` / `local_river` / `national_river` |
| `freq` | 30 / 50 / 80 / 100 / 200 / 500 / 기왕최대는 9999 |

### C

- 덤프 복원 후 바로 시작 가능. SQL 짤 필요 없다 — `route_summary()` 호출만.
- 기상청 특보 API 키 발급 (공공데이터포털, **자동승인**)
- `route_summary()` 에 `m_warn` / `m_extreme` 파라미터가 늘었다. **기본값이 있어 기존 호출은 그대로 동작한다.**
- ⚠️ 침수 빈도가 100년뿐인 동안에는 `alert=50` 을 넘겨도 침수 회피가 안 걸린다.
  데이터가 안 늘면 주의보에도 `100` 을 넘기는 것으로 정한다.

---

## 이미 겪은 문제 — 재발 방지

| 문제 | 원인 | 해결 |
|---|---|---|
| **경로가 항상 0행** | `ways_vertices_pgr` 에 `lon`/`lat` 칼럼이 있는데 함수 파라미터도 같은 이름 → 칼럼이 파라미터를 가림. 모든 행 거리가 0이 되어 항상 1번 노드 반환. **에러가 안 남** | 파라미터를 `p_lon`/`p_lat` 로. `99_check.sql` 이 재발 감지 |
| osm2pgrouting 설정 파싱 실패 | 배포판 mapconfig XML 주석 안에 `--` (XML 문법 위반) | 주석 줄 제거 후 사용 |
| 그래프 재생성 시 FK 충돌 | `manhole`·`risk_segment` 가 `ways` 참조 | 재생성 전 FK 해제 → 후 재설정 |
| 계수 튜닝마다 재계산 | `risk_segment` 에 가중치까지 저장했었음 | 가중치를 `route_safe()` 파라미터로 분리 |
| `docker cp` 경로 깨짐 | Git Bash 가 `/tmp` 를 윈도우 경로로 변환 | PowerShell 로 실행 |
| 한글 깨짐 | 콘솔이 CP949 | 파이썬으로 UTF-8 파일에 쓰고 읽기 |

---

## 설계에서 잊으면 안 되는 것

- **위험은 경로 탐색 이후 보정하는 게 아니라, 탐색의 입력 조건이다.**
  `cost = length_m × (1 + w_flood + w_manhole)`
- **회피 단위는 도로 구간이다.** GPS 오차가 3~10m라 맨홀 1m 비껴가기는 성립하지 않는다.
- **침수와 맨홀은 더한다. 곱하지 않는다.** 곱하면 한쪽이 0일 때 전체가 0이 된다.
  실제 데이터로 침수인데 맨홀 없는 구간 2,149개, 맨홀 있는데 침수 아닌 구간 5,463개다.
  곱셈은 "둘 다 있어야 위험", 덧셈은 "하나만 있어도 위험" — 우리는 후자다.
- **맨홀 가중치는 강우 강도에 비례한다.** 평상시 0 (뚜껑이 보이니 사람이 피한다),
  주의보 ×1.0, 경보 ×1.5, 극한호우 ×2.0. 배수는 `m_warn` / `m_extreme` 파라미터다.
- **경보를 켜면 맨홀 수가 늘 수 있다.** 침수를 피하려고 맨홀 골목으로 도는 것은 정상이다.
  판정 기준은 맨홀 수가 아니라 **침수 통과 길이**다. `99_check.sql` 이 이걸로 판정한다.
- **가중치는 DB에 저장하지 않는다.** 사실(`min_freq`, `manhole_count`)만 저장하고
  정책(배수)은 호출할 때 넘긴다.
- **맨홀 종류 구분이 없다.** 원본에 컬럼 자체가 없어 전부 위험으로 취급 중. 알려진 한계.
- **실시간 침수 데이터는 존재하지 않는다.** 면이면서 실시간인 공개 데이터가 없어
  정적 침수지도 + 기상 특보 2층 구조로 대체했다.
- **키링은 경로에 딸린 기능이 아니다.** 안전 경로는 이미 침수 구간을 피하므로
  그 경로 위에 경고할 맨홀이 **0개**다. 키링의 쓰임새는 경로를 벗어났거나
  경로를 안 쓰고 그냥 걸을 때다. 그래서 경로가 아니라 **현재 위치 반경**으로 판정한다.
- **키링 경고 대상은 침수 구간 맨홀만이다.** 전체 맨홀을 대상으로 하면 반경 15m
  기준 3.4km 에 166개가 걸려 **16초마다 울린다.** 침수 구간으로 좁히면 3개다.
  반경을 15m 밑으로 못 줄이는 이유는 GPS 오차가 3~10m 이기 때문이다.
