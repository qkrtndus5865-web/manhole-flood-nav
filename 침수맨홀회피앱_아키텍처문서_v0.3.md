# 맨홀 및 침수길 회피 보행 네비게이션

## 시스템 아키텍처 설계 문서 (v0.3)

작성일: 2026-08-08 | 분류: 시스템 구조 / 데이터 · API 설계 | 팀: 럭키신드롬

---

## 1. 개요

### 1.1 배경 및 목적

집중호우 시 하수관 내부 압력이 상승하면 맨홀 뚜껑이 밀려 올라가거나 이탈하는 경우가 있다. 도로가 이미 물에 잠긴 상태에서는 뚜껑이 열린 맨홀이 수면 아래에 가려져 보이지 않기 때문에, 보행자가 이를 인지하지 못하고 추락하는 사고가 반복적으로 발생해 왔다. 또한 맨홀과 무관하게 도로 자체가 통행 불가능한 수준으로 잠기는 구간도 보행자에게는 동일한 위험이다.

이 프로젝트는 이러한 침수기 보행 사고를 예방하는 것을 목적으로 하는 모바일 네비게이션 애플리케이션의 시스템 구조를 정의한다. 앱의 역할은 출발지에서 도착지까지의 보행 경로를 안내하되, **위험 맨홀과 침수 구간을 애초에 지나지 않는 경로**를 계산해 제시하는 것이다.

### 1.2 v0.2 대비 주요 변경 사항

v0.2는 "같은 보도 위에서 좌우로 1미터 비켜 가는" 정밀 회피를 핵심 기능으로 삼고, 이를 위해 상위 경로 탐색과 하위 좌표 보정을 분리한 2단 라우팅 구조를 채택했다. v0.3에서는 이 구조를 폐기하고 **도로 단위 우회로 회피 개념을 통일**한다. 변경의 근거와 파급 효과는 다음과 같다.

**첫째, 정밀 회피 폐기의 근거는 측위 정확도다.** v0.2 문서 5.2절이 스스로 지적했듯, 안내하려는 회피 거리(1미터)가 위치 측정 오차(개활지 3미터, 도심 10미터 이상)보다 작다. 사용자가 맨홀의 왼쪽에 있는지 오른쪽에 있는지조차 판별할 수 없는 상태에서 "왼쪽으로 1미터"를 안내하는 것은 정보가 아니라 소음이다. v0.2는 이 문제를 안내 등급 3단계로 완화하려 했으나, 이는 근본 해결이 아니라 "정밀 안내가 대부분의 환경에서 동작하지 않음"을 인정하는 장치에 가까웠다. 도로 단위 우회는 우회 단위가 수십에서 수백 미터이므로 측위 오차가 안내 품질을 좌우하지 않는다.

**둘째, 침수길이 회피 대상으로 승격되었다.** v0.2는 도로 전면 침수 구간을 "좌우 1미터로 해결되지 않는 별개의 문제"로 보아 스코프에서 제외하고 경고 표시 수준으로만 처리했다. 회피 단위가 도로로 올라간 지금은 맨홀과 침수 구간이 동일한 방식으로 처리된다. 둘 다 "지나지 말아야 할 도로 구간"이라는 하나의 개념으로 수렴한다.

**셋째, 라우팅 엔진을 자체 구축으로 전환한다.** 이 변경은 첫째 항목의 필연적 귀결이다. v0.2가 T맵 보행자 API를 채택한 논리는 "하위 계층인 정밀 회피 엔진이 회피를 전담하므로 상위 계층은 도로 가중치를 조작할 필요가 없다"는 것이었다. 회피 책임이 라우팅 계층으로 이동한 이상 이 전제는 성립하지 않는다. 위험 구간에 가중치를 부여하거나 통행을 금지하려면 경로 탐색 알고리즘 자체를 제어할 수 있어야 하는데, T맵을 포함한 상용 경로 API는 이러한 커스텀 비용 함수를 노출하지 않는다. 따라서 **pgRouting 기반 자체 경로 탐색**으로 전환한다. 선정 근거는 5장에서 상술한다.

**넷째, 안내 등급 3단계 구조를 폐기한다.** 좌우 판별이 사라졌으므로 일반적인 턴바이턴 안내로 단순화한다. 다만 위험 구간에 접근할 때의 경고는 유지한다. 경고는 반경 기반 판정이므로 측위 오차에 관대하다.

**다섯째, T맵 API의 역할을 축소하되 유지한다.** 경로 탐색 주체에서는 내려오지만 출발지·도착지 주소 및 POI 검색(지오코딩)에는 여전히 필요하며, 자체 라우팅이 실패하는 경우의 폴백으로도 활용한다. 기 신청된 API 이용 예산은 그대로 유효하다.

**여섯째, 모바일 클라이언트는 Flutter 단일 스택을 유지한다.** v0.2의 판단을 변경할 이유가 없다.

---

## 2. 스코프 정의

설계 이전에 이 앱이 다루는 위험과 다루지 않는 위험을 구분한다. v0.3에서는 회피 단위가 도로로 통일되면서 v0.2보다 범위가 넓어졌다.

**범위에 포함되는 위험은 두 종류다.** 하나는 침수구역과 겹치는 위치에 있는 우수 및 오수 맨홀이다. 관 내부 압력 상승으로 뚜껑이 이탈할 수 있고 수면 아래에 가려져 육안으로 식별되지 않는다. 다른 하나는 침수구역과 겹치는 보행로 구간 자체다. 수심과 유속을 알 수 없는 물길을 보행자가 통과하는 것은 맨홀 유무와 무관하게 위험하다.

v0.3에서 이 두 위험은 **동일한 처리 경로**를 탄다. 둘 다 "해당 보행로 구간의 통행 비용을 높이거나 통행을 금지하는" 방식으로 경로 탐색에 반영된다. 차이는 위험의 강도를 비용으로 환산하는 계수뿐이다.

**범위에서 제외되는 것은 다음과 같다.** 통신 맨홀과 전력 맨홀은 하수관과 달리 내부 수압 상승으로 뚜껑이 밀려 올라가는 구조가 아니어서 위험 판정 대상으로 삼으면 오탐만 늘어난다. 위험 구간을 산출하는 공간 질의 단계에서 맨홀 종류를 필터 조건으로 반드시 포함한다. 차량 통행은 다루지 않으며 보행 경로만 대상으로 한다. 실내 경로와 지하 통로 역시 다루지 않는다.

**경계 사례로 명시할 것이 하나 있다.** 출발지나 도착지 자체가 침수구역 안에 있는 경우, 또는 위험 구간을 통과하지 않고서는 도착할 수 없는 경우다. 이때 앱은 "경로 없음"을 반환하지 않고, 통행 금지를 완화한 최소 위험 경로를 계산해 **위험 구간을 지난다는 사실을 명시한 상태로** 제시한다. 처리 방식은 5.3절에서 정의한다.

---

## 3. 핵심 설계 원칙: 위험 가중 단일 라우팅

v0.3의 설계 원칙은 하나의 문장으로 요약된다. **위험은 경로 탐색 이후에 보정하는 대상이 아니라, 경로 탐색이 입력으로 받는 조건이다.**

v0.2의 2단 구조에서는 경로를 먼저 만들고 그 위에서 위험을 피했다. 이 방식은 "이미 정해진 길 위에서만" 회피가 가능하다는 근본적 한계를 갖는다. 그 길 전체가 잠겨 있으면 할 수 있는 일이 없다. v0.3에서는 순서를 뒤집는다. 위험 정보를 먼저 도로망에 반영하고, 그렇게 가중치가 부여된 도로망 위에서 최단 경로를 찾는다. 결과적으로 반환되는 경로는 **처음부터 안전한 하나의 경로**이며, 사후 보정 단계가 존재하지 않는다.

```
[1] 배치: 침수 데이터 + 맨홀 데이터
         → 보행로 구간별 위험도 산출 (RiskSegment)
                    ↓  (경로 요청과 무관하게 미리 계산됨)
[2] 요청: 출발지/도착지 좌표
         → 위험도가 반영된 비용으로 최단 경로 탐색 (pgr_dijkstra)
                    ↓
[3] 응답: 안전 경로 1개 + 경유한 위험 구간 목록 + 우회 사유
```

이 구조의 장점은 세 가지다.

**첫째, 측위 정확도로부터 독립적이다.** 안내 단위가 "다음 교차로에서 우회전" 수준이므로 3~10미터의 GPS 오차가 안내를 무효화하지 않는다. v0.2의 가장 큰 설계 제약이 구조적으로 해소된다.

**둘째, 위험의 종류가 늘어나도 구조가 변하지 않는다.** 침수길이든 맨홀이든, 향후 낙하물 위험이나 공사 구간이 추가되더라도 모두 "구간 비용에 더해지는 항"으로 흡수된다. 비용 함수의 계수만 늘어난다.

**셋째, 계산 시점을 분리할 수 있다.** 위험도 산출은 침수 데이터 갱신 주기(십 분~시간 단위)에 맞춰 배치로 수행하고, 경로 탐색은 요청 시점에 이미 계산된 비용을 읽기만 한다. 응답 지연이 위험 판정 로직의 복잡도에 영향받지 않는다.

---

## 4. 시스템 아키텍처

### 4.1 구성 요소

전체 시스템은 모바일 클라이언트, 백엔드 API 서버, 공간 데이터베이스, 데이터 수집 배치의 네 부분으로 구성된다. v0.2와 구성 요소의 수는 같지만 **무게 중심이 데이터베이스로 크게 이동**했다.

**모바일 클라이언트**는 Flutter로 개발하며 지도 렌더링, 실시간 위치 추적, 턴바이턴 안내 UI, 위험 구간 접근 경고를 담당한다. 통신이 불안정한 상황을 대비해 최근 조회한 경로와 주변 위험 구간 목록을 로컬에 캐싱한다. v0.2에 있던 좌우 회피 안내 UI와 안내 등급 판정 로직은 제거된다.

**백엔드 API 서버**는 경로 요청을 받아 출발지·도착지 좌표를 보행로 그래프의 최근접 노드로 스냅하고, pgRouting 질의를 실행해 경로를 얻은 뒤, 경로가 지나는 위험 구간 정보를 함께 묶어 반환한다. v0.2의 "정밀 회피 보정 엔진"은 존재하지 않으며, 그 자리를 **비용 함수 구성과 완화 전략 판단** 로직이 대신한다. 서버가 하는 일이 가벼워지고 대부분의 연산이 DB로 내려간다.

**공간 데이터베이스**는 PostgreSQL에 PostGIS와 pgRouting 확장을 함께 사용한다. v0.2에서는 공간 질의만 담당했으나 v0.3에서는 **경로 탐색까지 DB가 수행**한다. 이 프로젝트에서 가장 핵심적인 구성 요소다.

**데이터 수집 배치**는 외부 침수 데이터를 주기적으로 가져와 적재하고, 보행로 구간 및 맨홀과의 공간 조인을 통해 구간별 위험도 테이블을 갱신한다. 별도 오케스트레이션 도구 없이 스케줄러에 등록된 단일 파이썬 스크립트로 구현한다. 이 판단은 v0.2에서 유지한다. 침수 예보의 실제 갱신 주기가 분 단위가 아니므로 스트리밍 처리가 필요 없다.

### 4.2 데이터 흐름

데이터 흐름은 갱신 주기가 다른 세 갈래로 나뉜다.

**정적 계층 (초기 1회, 이후 드물게 갱신).** OpenStreetMap에서 보행로 네트워크를 추출해 `RoadSegment` 테이블에 적재하고 pgRouting 토폴로지(노드-엣지 연결 관계)를 구축한다. 이어서 맨홀 데이터를 각 구간에 스냅해 `Manhole.segment_id`를 채운다. 이 작업은 도로망이나 맨홀 데이터가 바뀔 때만 다시 수행한다.

**동적 계층 (10분~1시간 주기 배치).** 외부 공급원에서 침수구역 데이터를 수집해 `FloodZone`에 upsert하고, 만료된 레코드의 `is_active`를 해제한다. 그다음 활성 침수구역과 보행로 구간을 공간 조인하고, 각 구간에 걸린 위험 맨홀 수를 집계해 `RiskSegment`를 전면 재계산한다. 이 배치가 끝나면 도로망 전체에 최신 위험도가 반영된 상태가 된다.

**요청 계층 (사용자 요청 시).** 사용자가 출발지와 도착지를 입력하면 클라이언트가 API 서버에 경로를 요청한다. 서버는 두 좌표를 그래프 노드로 스냅한 뒤, `RoadSegment`와 `RiskSegment`를 조인해 만든 비용 뷰 위에서 `pgr_dijkstra`를 실행한다. 반환된 경로에 대해 통과하는 위험 구간을 재조회하여 응답에 포함시키고, 클라이언트는 이를 지도에 그린 뒤 실시간 위치와 대조해 안내와 경고를 제공한다.

```
[정적]  OSM 보행로 ──▶ RoadSegment (+ pgRouting 토폴로지)
        맨홀 데이터 ──▶ Manhole ──스냅──▶ segment_id

[동적]  침수 데이터 ──▶ FloodZone ──┐
                                    ├─공간 조인─▶ RiskSegment
                        Manhole ────┘              (구간별 위험도)

[요청]  출발/도착 좌표 ──스냅──▶ 노드
                          ↓
              RoadSegment ⋈ RiskSegment = 비용 뷰
                          ↓
                    pgr_dijkstra
                          ↓
              경로 + 통과 위험 구간 + 완화 여부
```

---

## 5. 위험 비용 모델과 경로 탐색

### 5.1 비용 함수

경로 탐색의 핵심은 각 보행로 구간에 부여하는 통행 비용이다. 기본 원리는 **실제 거리에 위험 계수를 곱해 "체감 거리"를 만드는 것**이다. 위험한 구간은 실제보다 길게 느껴지도록 만들어, 최단 경로 알고리즘이 자연스럽게 그 구간을 피하게 한다.

```
cost = length_m × (1 + w_flood + w_manhole)
```

`w_flood`는 침수 위험도에 따른 가산 계수이고, `w_manhole`은 해당 구간에 걸린 위험 맨홀 수에 따른 가산 계수다. 초기 계수는 다음과 같이 설정하되, 데모 구역에서 실제 경로를 뽑아 보며 조정한다.

| 조건 | 계수 | 의미 |
|---|---|---|
| 침수 없음 | `w_flood = 0` | 실제 거리 그대로 |
| 침수 주의 | `w_flood = 0.5` | 1.5배 길게 체감 |
| 침수 경계 | `w_flood = 2.0` | 3배 길게 체감 |
| 침수 심각 | 통행 금지 | 비용 `-1` (pgRouting에서 통행 불가) |
| 위험 맨홀 n개 | `w_manhole = min(0.3 × n, 1.5)` | 맨홀당 가산, 상한 존재 |

맨홀 계수에 상한을 두는 이유는, 한 구간에 맨홀이 많다고 해서 위험이 선형으로 커지지는 않기 때문이다. 맨홀이 5개든 20개든 "지나지 않는 편이 낫다"는 판단은 같다.

계수의 의미를 직관적으로 보면, `w_flood = 2.0`인 100미터 구간은 300미터로 계산된다. 즉 **300미터 미만의 우회로가 존재하면 알고리즘이 우회를 택한다.** 계수 조정이란 결국 "이 위험을 피하기 위해 몇 미터까지 더 걸을 의향이 있는가"를 정하는 일이다. 이 관점에서 계수를 설명하면 팀 내 합의와 발표 설명이 모두 쉬워진다.

### 5.2 경로 탐색

pgRouting의 `pgr_dijkstra`를 사용한다. 비용은 `RoadSegment`와 `RiskSegment`를 LEFT JOIN한 뷰에서 계산하며, 위험 정보가 없는 구간은 계수 0으로 처리된다.

```sql
-- 개념 예시 (실제 구현 시 파라미터화)
SELECT * FROM pgr_dijkstra(
  $$ SELECT s.seg_id AS id, s.source, s.target,
            CASE WHEN r.is_blocked THEN -1
                 ELSE s.length_m * (1 + COALESCE(r.w_flood,0)
                                      + COALESCE(r.w_manhole,0))
            END AS cost
       FROM road_segment s
       LEFT JOIN risk_segment r ON r.seg_id = s.seg_id $$,
  :start_node, :end_node, directed := false
);
```

보행 경로이므로 `directed := false`로 양방향 통행을 허용한다. 차량 경로와 달리 일방통행 개념이 없기 때문이다.

### 5.3 통행 금지 완화 전략

통행 금지 구간을 두면 필연적으로 **경로가 존재하지 않는 상황**이 발생한다. 도착지가 침수구역 한복판에 있거나, 목적지로 가는 모든 길목이 잠긴 경우다. 이때 "경로 없음"만 반환하는 것은 사용자에게 아무 도움이 되지 않는다. 다음 2단계로 처리한다.

**1차 탐색(엄격 모드).** 심각 등급 침수 구간을 통행 금지(`-1`)로 두고 탐색한다. 경로가 나오면 그대로 반환하며 응답의 `degraded` 플래그는 `false`다.

**2차 탐색(완화 모드).** 1차에서 경로가 없으면 통행 금지를 해제하고 대신 매우 큰 패널티(`w_flood = 20`, 즉 21배 체감)를 적용해 재탐색한다. 경로가 나오면 `degraded: true`와 함께 어느 구간이 위험한지를 명시해 반환한다. 클라이언트는 이 경우 경로를 붉게 표시하고 "안전한 우회로가 없어 위험 구간을 포함한 경로를 안내합니다"를 상단에 고정 노출한다.

**2차에서도 실패하면** 그래프상 연결 자체가 없는 경우이므로 "경로를 찾을 수 없음"을 반환한다. 이는 위험이 아니라 데이터 문제(보행로 그래프 단절)일 가능성이 높으므로 로그를 남겨 데이터 품질 점검에 활용한다.

큰 패널티를 쓰되 무한대를 쓰지 않는 이유는, 완화 모드에서도 알고리즘이 **위험 구간을 가능한 한 짧게 지나는 경로**를 선택하도록 만들기 위해서다. 어차피 지나야 한다면 최소한으로 지나는 것이 낫다.

### 5.4 위험 구간 접근 경고

경로가 위험 구간을 포함하는 경우(주로 완화 모드), 클라이언트는 실시간 위치가 해당 구간 시작점으로부터 일정 반경 안에 들어오면 경고를 표시한다. 반경은 30미터를 기본값으로 하며, 이는 일반적인 GPS 오차(3~10미터)보다 충분히 크므로 측위 정확도가 나빠도 경고 자체는 안정적으로 동작한다. **v0.2의 안내 등급 3단계 구조는 필요 없어졌고, 이 단일 반경 판정이 그 역할을 대신한다.**

---

## 6. 데이터 확보 전략

v0.3에서는 필요한 원본 데이터가 **두 종류에서 세 종류로 늘었다.** 도로망 그래프가 새로 추가되었기 때문이다. 이는 자체 라우팅 전환의 대가이며, 데이터 담당 업무의 무게 중심이 이동하는 지점이다.

### 6.1 보행로 네트워크 (신규)

OpenStreetMap에서 추출한다. 경로 탐색의 기반이므로 이 데이터가 없으면 아무것도 동작하지 않는다.

- **대상 태그**: `highway=footway`, `sidewalk`, `pedestrian`, `path`, `steps`, `living_street`, 그리고 인도가 별도로 그려지지 않은 지역을 위해 `residential`·`service`의 일부
- **도구**: `osm2pgrouting` 또는 `osm2po`로 추출과 동시에 pgRouting 토폴로지를 생성
- **필수 검증**: 토폴로지 구축 후 `pgr_analyzeGraph`로 고립 노드와 단절 구간을 점검한다. 국내 OSM은 차도는 잘 그려져 있으나 **인도가 별도 라인으로 등록되지 않은 지역이 많으므로**, 데모 구역을 확정하기 전에 반드시 그래프 연결성을 확인해야 한다.

이 검증은 프로젝트 초기에 **가장 먼저 수행해야 할 작업**이다. 데모 구역의 보행로 그래프가 단절되어 있으면 회피 로직이 아무리 잘 동작해도 경로가 나오지 않는다.

### 6.2 침수구역 데이터

v0.2의 전략을 유지한다. 행정안전부 재해안전데이터 공유플랫폼, 홍수위험지도 정보시스템, 지자체 열린데이터 포털의 침수예상도를 후보로 검토한다. 예보 기반 데이터와 실측 기반 데이터는 신뢰도와 갱신 주기가 다르므로 `source_type`으로 구분해 저장하고 위험도 등급 산정에 반영한다.

v0.3에서 추가되는 요구사항은 **폴리곤 품질**이다. 회피 단위가 도로로 올라가면서 침수 폴리곤과 보행로 구간의 교차 판정이 경로 결과를 직접 좌우하게 되었다. 폴리곤이 지나치게 크게 잡히면 광범위한 지역이 통행 금지가 되어 경로가 나오지 않고, 지나치게 작으면 위험을 놓친다. 실제 데이터를 받아본 뒤 필요하면 **버퍼 축소나 등급 재분류**를 전처리 단계에 넣는다.

### 6.3 맨홀 위치 데이터

v0.2와 동일하게 전국 단위 공개 데이터셋이 존재하지 않는 것이 최대 불확실 요소다. 네 가지 경로를 병행한다.

| 순위 | 방법 | 특징 |
|---|---|---|
| 1 | OpenStreetMap `man_made=manhole` 태그를 Overpass API로 조회 | 비용·절차 없이 즉시 시도 가능. 국내 등록 밀도가 지역마다 편차가 큼 |
| 2 | 데모 구역을 좁게 한정하고 팀이 직접 현장 좌표 수집 | 커버리지 부족 시 캠퍼스 주변 규모로 축소 |
| 3 | 지자체 상하수도 부서 정보공개청구로 관로 대장 확보 | 품질 최상이나 처리 기간이 학기 일정을 넘길 위험 |
| 4 | 국가공간정보포털 지하시설물 레이어 신청 | 병행 신청 |

현실적인 진행 순서는 OpenStreetMap 조회로 대상 지역 커버리지를 먼저 확인하고, 부족하면 데모 구역을 좁혀 직접 수집으로 보완하는 것이다. 정보공개청구와 포털 신청은 병행하되 **회신 시점을 일정에 반영하지 않는다.** 또한 공식 데이터의 누락을 보완하기 위해 사용자가 앱에서 직접 위험 지점을 신고하는 기능을 두어, 데이터 커버리지 문제를 제품 기능으로 흡수하는 방향을 함께 설계한다.

v0.3에서 추가되는 작업은 **맨홀-구간 스냅**이다. 맨홀은 점 데이터이지만 비용은 선 구간에 부여되므로, 각 맨홀을 최근접 보행로 구간에 매핑해 두어야 한다. `ST_ClosestPoint`와 거리 임계값(예: 15미터)을 사용하며, 임계값을 넘는 맨홀은 보행로와 무관한 것으로 보아 매핑에서 제외한다.

---

## 7. 데이터 모델

### 7.1 RoadSegment (신규)

pgRouting 경로 탐색의 기반이 되는 보행로 구간 테이블이다. `source`와 `target`은 토폴로지 구축 시 자동 생성되는 노드 식별자다.

| 필드 | 타입 | 설명 |
|---|---|---|
| `seg_id` | BIGINT (PK) | 구간 고유 식별자 |
| `source` | BIGINT | 시작 노드 식별자 (토폴로지 생성) |
| `target` | BIGINT | 종료 노드 식별자 (토폴로지 생성) |
| `geom` | geometry(LineString, 4326) | 구간 형상 |
| `length_m` | DOUBLE PRECISION | 구간 실제 길이(미터) |
| `way_type` | VARCHAR | footway / sidewalk / crossing / steps 등 |
| `osm_id` | BIGINT | 원본 OSM 식별자 (추적용) |

### 7.2 Manhole

v0.2에서 `segment_id`가 추가되었다. 스냅 결과를 저장해 위험도 집계 시 조인 비용을 줄인다.

| 필드 | 타입 | 설명 |
|---|---|---|
| `manhole_id` | BIGINT (PK) | 맨홀 고유 식별자 |
| `location` | geometry(Point, 4326) | 맨홀 위경도 좌표 |
| `manhole_type` | VARCHAR | 우수 / 오수 / 통신 / 전력. 위험 판정 필터 기준 |
| `segment_id` | BIGINT (FK) | **[신규]** 스냅된 보행로 구간. NULL이면 매핑 실패 |
| `source` | VARCHAR | 데이터 출처 (OSM, 지자체, 사용자 신고 등) |
| `confidence` | VARCHAR | 출처별 신뢰 수준 |
| `updated_at` | TIMESTAMP | 최종 갱신 시각 |

### 7.3 FloodZone

v0.2에서 변경 없음.

| 필드 | 타입 | 설명 |
|---|---|---|
| `flood_zone_id` | BIGINT (PK) | 침수구역 고유 식별자 |
| `area` | geometry(Polygon, 4326) | 침수 영역 폴리곤 |
| `risk_level` | VARCHAR | 위험도 등급 (주의 / 경계 / 심각) |
| `source_type` | VARCHAR | 예보 기반 / 실측 기반 구분 |
| `is_active` | BOOLEAN | 현재 유효 여부 |
| `observed_at` | TIMESTAMP | 관측 또는 예보 시각 |
| `expires_at` | TIMESTAMP | 유효 종료 시각 |

### 7.4 RiskSegment (v0.2의 RiskManhole 대체)

v0.2의 `RiskManhole`은 "위험한 맨홀 목록"이었으나, v0.3에서는 **"위험한 도로 구간 목록"**으로 개념이 바뀐다. 침수길과 맨홀 위험이 하나의 테이블로 통합되는 지점이며, 이 테이블이 곧 경로 탐색의 비용 입력이 된다. 배치가 주기적으로 전면 재계산한다.

| 필드 | 타입 | 설명 |
|---|---|---|
| `seg_id` | BIGINT (PK, FK) | 대상 보행로 구간 |
| `flood_level` | VARCHAR | 교차한 침수구역 중 최고 등급 (없으면 NULL) |
| `w_flood` | DOUBLE PRECISION | 침수 위험 계수 |
| `manhole_count` | INTEGER | 구간에 걸린 위험 맨홀 수 (우수·오수만) |
| `w_manhole` | DOUBLE PRECISION | 맨홀 위험 계수 |
| `is_blocked` | BOOLEAN | 통행 금지 여부 (심각 등급) |
| `computed_at` | TIMESTAMP | 위험 판정 계산 시각 |

한 구간이 여러 침수구역과 겹칠 수 있으므로 `flood_level`은 **최고 등급을 승계**한다. 위험 판정은 보수적으로 하는 것이 원칙이다.

### 7.5 Route

v0.2에서 `base_path`/`refined_path` 2중 구조가 사라지고 단일 경로로 단순화된다. 사후 보정 단계가 없어졌기 때문이다.

| 필드 | 타입 | 설명 |
|---|---|---|
| `route_id` | UUID (PK) | 경로 요청 결과 식별자 |
| `path` | geometry(LineString, 4326) | 계산된 보행 경로 |
| `total_distance_m` | DOUBLE PRECISION | 실제 거리 합계 |
| `total_cost` | DOUBLE PRECISION | 위험 가중 비용 합계 |
| `degraded` | BOOLEAN | 완화 모드로 계산되었는지 여부 |
| `passed_risk_segments` | JSON | 경로가 지나는 위험 구간 식별자와 사유 |
| `created_at` | TIMESTAMP | 경로 생성 시각 |

`total_distance_m`와 `total_cost`를 함께 보관하는 이유는, 두 값의 비율이 "이 경로가 위험을 피하느라 얼마나 돌아갔는지"를 나타내는 지표가 되어 계수 튜닝과 결과 검증에 유용하기 때문이다.

### 7.6 Report

사용자 신고. v0.2에서 변경 없음.

| 필드 | 타입 | 설명 |
|---|---|---|
| `report_id` | BIGINT (PK) | 신고 식별자 |
| `location` | geometry(Point, 4326) | 신고 위치 |
| `description` | TEXT | 선택적 설명 |
| `reported_at` | TIMESTAMP | 신고 시각 |

---

## 8. API 설계

REST 방식으로 설계하며 모든 좌표는 **경도, 위도 순서**의 배열 또는 GeoJSON 형식을 따른다. PostGIS의 `ST_MakePoint(lon, lat)` 및 GeoJSON 표준과 일치시켜 좌표 순서 혼동을 줄인다.

| Method / Path | 설명 | 주요 파라미터 및 응답 |
|---|---|---|
| `GET /v1/segments/risk` | 지정 영역 내 위험 보행로 구간 조회 | 요청: `bbox`. 응답: 구간 형상과 위험 계수, 통행 금지 여부 |
| `GET /v1/flood-zones` | 현재 활성 침수구역 조회 | 요청: `bbox`, `active`. 응답: 폴리곤과 위험 등급 |
| `GET /v1/manholes` | 지정 영역 내 맨홀 조회 | 요청: `bbox`, `type`. 지도 표시 및 디버깅용 |
| `POST /v1/routes` | 출발·도착 좌표로 회피 경로 생성 | 요청: 출발지·도착지 좌표. 응답: 경로 식별자, 경로 형상, 거리, 소요 예상 시간, `degraded` 플래그, 통과 위험 구간 배열 |
| `GET /v1/routes/{id}` | 생성된 경로 재조회 | 응답: 경로 생성과 동일한 객체 |
| `POST /v1/reports` | 사용자 위험 지점 신고 등록 | 요청: 좌표와 선택적 설명. 신뢰 수준은 신고 출처로 기록 |

v0.2 대비 변경점은 다음과 같다. `GET /v1/manholes/risk`가 `GET /v1/segments/risk`로 대체되었다. 위험의 단위가 점에서 선으로 바뀌었기 때문이다. `POST /v1/routes` 응답에서 `base_path`와 `avoidance_points`가 사라지고 단일 `path`와 `degraded`, `passed_risk_segments`가 들어왔다.

---

## 9. 기술 스택

| 영역 | 선택 | 근거 |
|---|---|---|
| 모바일 | Flutter | 네이티브 이중 개발은 학기 내 비현실적. v0.2 판단 유지 |
| 백엔드 | Python + FastAPI | 공간 연산 대부분을 DB에 위임하므로 서버 언어의 성능 영향이 작음. 데이터 수집 배치와 언어를 통일해 코드 재사용 |
| DB | PostgreSQL + PostGIS + **pgRouting** | 공간 질의와 경로 탐색을 한 DB에서 처리 |
| 경로 탐색 | **pgRouting `pgr_dijkstra`** | 아래 상술 |
| 지오코딩·폴백 | T맵 API | 주소·POI 검색 및 자체 라우팅 실패 시 폴백 |
| 도로망 추출 | `osm2pgrouting` | OSM에서 추출과 토폴로지 생성을 동시 수행 |
| 배치 | 스케줄러 + 단일 파이썬 스크립트 | 침수 데이터 갱신 주기가 십 분~시간 단위. 오케스트레이션 도구 불필요 |

### 라우팅 엔진 선정 근거

자체 라우팅으로 전환하면서 세 가지 후보를 검토했다.

**OSRM은 부적합하다.** 가중치를 변경하려면 `osrm-extract`와 `osrm-contract`로 그래프 전체를 재전처리해야 하는데, 이 과정은 도시 규모에서 수 분 이상 걸린다. 침수 상황은 십 분 단위로 바뀌므로 매 배치마다 전체 재전처리를 도는 것은 현실적이지 않다. v0.1이 OSRM을 채택했다가 v0.2에서 버린 이유와는 다르지만, 결론은 같다.

**GraphHopper는 가능하나 과하다.** `custom_model`로 요청 시점 동적 가중치를 지원해 요구사항 자체는 충족한다. 그러나 JVM 기반 별도 서버를 띄워야 하고, 위험 데이터를 DB에서 꺼내 요청마다 모델로 전달하는 연동 코드가 필요하다. 학기 프로젝트 규모에서 학습 비용 대비 이득이 크지 않다.

**pgRouting을 채택한다.** 결정적 이유는 **위험 데이터와 경로 탐색이 같은 데이터베이스 안에 있다는 점**이다. 비용은 SQL 질의 안에서 `RiskSegment`를 조인해 즉석에서 계산되므로, 배치가 위험도를 갱신하는 순간 별도 전처리나 동기화 없이 다음 경로 요청부터 바로 반영된다. 추가 인프라가 전혀 필요 없고, 이미 PostGIS를 쓰기로 한 결정과 자연스럽게 이어진다.

**성능은 문제되지 않는다.** pgRouting은 대륙 규모 그래프에서는 전용 엔진보다 느리지만, 이 프로젝트의 대상은 데모 구역 내지 시 단위다. 수만 개 수준의 엣지에서 `pgr_dijkstra`는 수십 밀리초 안에 응답한다. 향후 범위가 넓어지면 `pgr_aStar`로 교체해 휴리스틱을 적용할 수 있으며, 인터페이스가 거의 동일해 교체 비용이 낮다.

---

## 10. 한계 및 향후 과제

**첫째, 보행로 그래프의 품질이 새로운 최대 리스크다.** v0.2에서는 맨홀 데이터 커버리지가 1순위 리스크였으나, 자체 라우팅으로 전환하면서 도로망 데이터가 그 자리를 나눠 갖게 되었다. 국내 OSM은 차도 커버리지는 양호하나 인도가 별도 라인으로 등록되지 않은 지역이 많다. 인도 데이터가 없으면 차도 중심선을 대체로 쓰게 되는데, 이 경우 경로가 실제 보행 가능 경로와 어긋난다. **데모 구역 확정 전에 그래프 연결성 검증을 반드시 선행해야 한다.**

**둘째, 맨홀 위치 데이터 커버리지 문제는 여전하다.** 공개 데이터가 충분하지 않아 초기 버전은 특정 시범 구역에 한정될 가능성이 높다. 사용자 신고 기능과 지자체 데이터 확보를 통해 단계적으로 완화해야 한다.

**셋째, 침수 데이터의 시간 해상도가 실시간에 못 미친다.** 예보 기반 데이터는 실제 침수 발생과 시차가 있고, 십 분~시간 단위 갱신은 급격한 상황 변화를 따라가지 못한다. 사용자 신고를 실시간 보정 채널로 활용하는 방향과, 향후 CCTV·수위계 등 실측 센서 데이터를 연동하는 방향을 검토할 수 있다.

**넷째, 비용 계수의 근거가 경험적이다.** 5.1절의 계수는 이론적 근거가 아니라 "이 위험을 피하려고 몇 미터를 더 걸을 것인가"에 대한 팀의 판단이다. 데모 구역에서 다양한 출발-도착 쌍의 경로를 뽑아 보며 조정해야 하며, 가능하다면 사용자 설정으로 "안전 우선 / 거리 우선" 프로필을 노출하는 것이 정직한 접근이다.

**다섯째, 정밀 회피 기능의 상실은 의도된 트레이드오프다.** 보도 폭 데이터가 확보되고 고정밀 측위(RTK, 이중 주파수 GNSS)가 보급되면 v0.2의 정밀 회피가 다시 유효해질 수 있다. 그때는 v0.3의 도로 단위 회피 위에 선택적 상위 기능으로 얹는 형태가 될 것이다. 두 방식은 배타적이지 않으며, **현 시점의 측위 환경에서 동작하지 않는 기능을 핵심에 두지 않았다는 것**이 v0.3의 판단이다.

---

## 부록 A. v0.2 → v0.3 변경 요약

| 항목 | v0.2 | v0.3 |
|---|---|---|
| 회피 단위 | 좌우 1미터 오프셋 (정밀 회피) | 도로 구간 우회 |
| 라우팅 구조 | 2단 (T맵 + 자체 보정 엔진) | 단일 (위험 가중 pgRouting) |
| 경로 탐색 | T맵 보행자 API | pgRouting `pgr_dijkstra` |
| 침수길 처리 | 스코프 제외, 경고만 | 회피 대상에 포함 |
| GPS 오차 대응 | 안내 등급 3단계 | 구조적으로 불필요 (반경 경고만) |
| 위험 테이블 | `RiskManhole` (점) | `RiskSegment` (선) |
| 필요 원본 데이터 | 침수구역, 맨홀 | 침수구역, 맨홀, **보행로 네트워크** |
| 경로 결과 | 기본 경로 + 상세 경로 | 단일 경로 + `degraded` 플래그 |
| T맵 역할 | 경로 탐색 주체 | 지오코딩 및 폴백 |

## 부록 B. 데이터 담당 우선 작업 순서

1. **보행로 그래프 연결성 검증** — 데모 후보 구역의 OSM 보행로를 추출해 `pgr_analyzeGraph`로 단절 여부 확인. 이 결과에 따라 데모 구역이 결정된다.
2. **맨홀 커버리지 확인** — 동일 구역에 Overpass API로 `man_made=manhole` 조회. 결과 수량으로 직접 수집 필요 여부 판단.
3. **침수구역 데이터 수급** — 후보 3개 채널의 제공 형식(SHP/GeoJSON/API)과 갱신 주기 확인.
4. **스키마 구축** — 7장 테이블 DDL 작성 및 공간 인덱스 생성.
5. **배치 스크립트 구현** — 수집 → 적재 → `RiskSegment` 재계산 파이프라인.
6. **계수 튜닝** — 데모 구역에서 경로 샘플을 뽑아 `total_cost / total_distance_m` 비율로 우회 정도 검증.
