# A역할 작업 기록

**대상: 경기도 의정부시** · 갱신 2026-08-12

## 대상이 서울 → 의정부로 바뀐 이유

맨홀 위치 데이터를 **의정부시 것만 구할 수 있었다.** 맨홀 회피가 이 앱의 핵심 기능이라
맨홀 데이터가 있는 곳이 대상이 될 수밖에 없다.

바꾸면서 오히려 좋아진 점이 있다.

| | 서울 | 의정부 |
|---|---|---|
| 맨홀 데이터 | 2010~2014년 | **2025-06-30** — 훨씬 최신 |
| 맨홀 좌표계 | EPSG:5186 (변환 필요) | **이미 위경도** — 변환 불필요 |
| 침수 SHP 수집 | 25개 자치구 × 3종 = 최대 75개 | **1개 시군구 × 3종 = 3개** |
| 그래프 크기 | 엣지 399,214 | 엣지 39,490 — 10분의 1 |

특히 **침수 SHP가 3개로 줄어든 게 크다.** 서울이었으면 25개 구를 하나씩 받아야 했다.

## 현재 상태

| 단계 | 상태 | 결과 |
|---|---|---|
| 0 환경 | 완료 | PostgreSQL 17.5 / PostGIS 3.5 / pgRouting 3.7.3 |
| 1 스키마 | 완료 | manhole · flood_zone · risk_segment |
| 2 보행 그래프 | 완료 | **엣지 39,490 / 노드 29,744 / 2,760km** |
| 3 침수구역 | **대기** | 의정부시 침수 SHP 3종 필요 |
| 4 맨홀 | 완료 | **20,873개 적재 · 스냅률 94.9%** |
| 5 위험도 | 완료(맨홀만) | 7,357개 구간에 맨홀 |
| 6 경로 함수 | 완료 | 290ms |
| 7 API | 시작 가능 | C 담당 |

### 실제 데이터로 회피 확인됨

의정부역 → 회룡역:

```
평상시   alert=0     2,377m   맨홀 6개 통과
호우경보 alert=100   2,380m   맨홀 1개 통과   ← 3m 더 걷고 5개 회피
```

가짜 데이터가 아니라 **실제 맨홀 2만 개로 나온 결과다.**

## 맨홀 데이터

`경기도 의정부시_맨홀 현황_20250630.csv` (공공데이터포털)

```
20,873행 · CP949 · 컬럼 7개
관리번호 / 입력일자 / 입력자 / 최종수정일자 / 최종수정자 / 경도 / 위도
좌표 범위  경도 127.006~127.122 · 위도 37.687~37.777
좌표 이상  0건
```

⚠️ **우수/오수 구분 컬럼이 없다.** 원본에 종류 정보 자체가 없어서 전부 위험 맨홀로 취급한다.
하수도 맨홀 현황 자료이므로 대부분 우수·오수일 것으로 보이나 확인은 불가하다.
설계 문서와 발표에 한계로 명시할 것.

## 그래프

```
south-korea-latest.osm.pbf   272MB
  └ 의정부 bbox 126.97,37.66,127.16,37.80
      └ 보행 도로만 필터   ways 18,122 / nodes 118,336
          └ osm2pgrouting    4.4초
```

재현: `sql/00_build_graph.sh` (bbox 변수만 바꾸면 다른 지역도 가능)

## 남은 일

1. **의정부시 침수 SHP 3종** — 도시침수 100년(sn=36) · 지방하천 100년(sn=23) · 국가하천 100년(sn=9)
   시군구가 하나뿐이라 파일 3개면 끝난다.
2. 침수 적재 후 `sql/04_risk.sql` 재실행
3. 덤프 배포
4. 기상청 특보 API 키 발급 (C)

## 겪은 문제와 해결

| 문제 | 원인 | 해결 |
|---|---|---|
| 이미지 다운로드 `unexpected EOF` | 네트워크 끊김 | `docker pull` 재시도 (이어받기) |
| osm2pgrouting 설정 파싱 실패 | 배포판 mapconfig XML 주석 안에 `--` (XML 문법 위반) | 주석 줄 제거 |
| 경로가 항상 0행 | `ways_vertices_pgr` 에 `lon`/`lat` 칼럼이 있는데 함수 파라미터도 같은 이름 → **칼럼이 파라미터를 가림**. 모든 행 거리가 0이 되어 항상 1번 노드 반환. **에러가 안 남** | 파라미터를 `p_lon`/`p_lat` 로 변경. `sql/99_check.sql` 에 재발 감지 추가 |
| 가중치 튜닝 때마다 재계산 | `risk_segment` 에 가중치까지 저장했음 | 가중치를 `route_safe()` 파라미터로 분리. 사실만 저장 |
| 그래프 재생성 시 FK 충돌 | `manhole`·`risk_segment` 가 `ways` 를 참조 | 재생성 전 FK 해제 → 후 재설정 |

## 접속 정보

```
localhost:5432 · db floodnav · user postgres · pw flood1234
컨테이너 재시작:  docker start flood-db
점검:            docker exec -i flood-db psql -U postgres -d floodnav < sql/99_check.sql
```
